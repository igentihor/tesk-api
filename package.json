{
  "name": "tesk-api",
  "version": "1.0.0",
  "description": "REST API for a todo/task manager — CRUD endpoints, filtering by status/priority. In-memory store, no DB needed.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build": "rm -rf dist && mkdir -p dist && cp -r src package.json package-lock.json dist/ && echo 'Build successful'",
    "lint": "eslint .",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "express",
    "rest-api",
    "todo",
    "task-manager",
    "crud"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
